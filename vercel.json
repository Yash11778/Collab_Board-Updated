{
  "buildCommand": "cd hackathon && npm install && npm run build",
  "outputDirectory": "hackathon/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
